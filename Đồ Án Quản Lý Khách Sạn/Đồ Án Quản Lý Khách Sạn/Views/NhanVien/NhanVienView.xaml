<UserControl x:Class="Đồ_Án_Quản_Lý_Khách_Sạn.Views.NhanVien.NhanVienView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:Đồ_Án_Quản_Lý_Khách_Sạn.Helpers"
             Background="{StaticResource BgBrush}">
    <UserControl.Resources>
        <conv:VaiTroTextConverter          x:Key="VaiTro"/>
        <conv:BoolToVisibilityConverter    x:Key="BoolVis"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <!-- Search inline -->
                <Border CornerRadius="8" BorderBrush="#BDBDBD" BorderThickness="1"
                        Background="White" Height="36" VerticalAlignment="Center" Margin="0,0,12,0">
                    <StackPanel Orientation="Horizontal" Margin="10,0">
                        <TextBlock Text="🔍" FontSize="12" Foreground="#90A4AE" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Width="180" BorderThickness="0" Background="Transparent"
                                 VerticalContentAlignment="Center" FontSize="13"/>
                    </StackPanel>
                </Border>
                <Button Style="{StaticResource BtnPrimary}" Command="{Binding ThemCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="+" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="Thêm Nhân Viên" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource BtnWarning}" Command="{Binding SuaCommand}" Margin="8,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✎" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="Sửa" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource BtnInfo}" Command="{Binding DoiMatKhauCommand}" Margin="8,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔑" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="Đổi Mật Khẩu" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource BtnDanger}" Command="{Binding DoiTrangThaiCommand}" Margin="8,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⊘" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="Đổi Trạng Thái" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
            <StackPanel>
                <TextBlock Style="{StaticResource H1}" Text="Nhân Viên"/>
                <TextBlock Style="{StaticResource Body}" Text="Quản lý tài khoản nhân viên (chỉ Admin)"/>
            </StackPanel>
        </DockPanel>

        <!-- Grid -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="0" ClipToBounds="True">
            <DataGrid ItemsSource="{Binding NhanViens}" SelectedItem="{Binding Selected}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã NV"     Binding="{Binding MaNV}"      Width="70"/>
                    <DataGridTextColumn Header="Họ Tên"    Binding="{Binding HoTen}"     Width="*"/>
                    <DataGridTextColumn Header="Tài Khoản" Binding="{Binding TaiKhoan}"  Width="120"/>
                    <DataGridTextColumn Header="Vai Trò"   Binding="{Binding VaiTro, Converter={StaticResource VaiTro}}" Width="130"/>
                    <DataGridTextColumn Header="Email"     Binding="{Binding Email}"     Width="*"/>
                    <DataGridTextColumn Header="SĐT"       Binding="{Binding SDT}"       Width="120"/>
                    <DataGridTemplateColumn Header="Trạng Thái" Width="120" SortMemberPath="IsActive">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12" Padding="10,3" HorizontalAlignment="Left">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#E8F5E9"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                    <Setter Property="Background" Value="#FFEBEE"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock FontSize="11" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="Hoạt Động"/>
                                                <Setter Property="Foreground" Value="#2E7D32"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Text" Value="Vô Hiệu"/>
                                                        <Setter Property="Foreground" Value="#C62828"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Ngày Tạo" Binding="{Binding NgayTao, StringFormat=dd/MM/yyyy}" Width="110"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
