<UserControl x:Class="Đồ_Án_Quản_Lý_Khách_Sạn.Views.HoaDon.HoaDonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:Đồ_Án_Quản_Lý_Khách_Sạn.Helpers"
             Background="{StaticResource BgBrush}">
    <UserControl.Resources>
        <conv:CurrencyConverter              x:Key="Currency"/>
        <conv:HoaDonTrangThaiColorConverter  x:Key="TTColor"/>
        <conv:HoaDonTrangThaiTextConverter   x:Key="TTText"/>
        <conv:PhuongThucTTTextConverter      x:Key="PTText"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Style="{StaticResource BtnSuccess}" Command="{Binding ThanhToanCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💳" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="Thanh Toán" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource BtnInfo}" Command="{Binding XemChiTietCommand}" Margin="8,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="👁" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="Xem Chi Tiết" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
            <StackPanel>
                <TextBlock Style="{StaticResource H1}" Text="Hóa Đơn"/>
                <TextBlock Style="{StaticResource Body}" Text="Quản lý thanh toán và hóa đơn"/>
            </StackPanel>
        </DockPanel>

        <!-- Filters -->
        <Border Grid.Row="1" Style="{StaticResource CardSmall}" Margin="0,0,0,16">
            <WrapPanel>
                <StackPanel Width="240" Margin="0,0,16,0">
                    <TextBlock Text="Tìm kiếm" Style="{StaticResource Label}"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Tag="Mã HĐ, tên khách, phòng…"/>
                </StackPanel>
                <StackPanel Width="160" Margin="0,0,16,0">
                    <TextBlock Text="Trạng thái" Style="{StaticResource Label}"/>
                    <ComboBox SelectedValue="{Binding FilterTrangThai}" SelectedValuePath="Tag">
                        <ComboBoxItem Content="Tất cả"          Tag="TatCa" IsSelected="True"/>
                        <ComboBoxItem Content="Chưa Thanh Toán" Tag="ChuaThanhToan"/>
                        <ComboBoxItem Content="Đã Thanh Toán"   Tag="DaThanhToan"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Width="140" Margin="0,0,16,0">
                    <TextBlock Text="Từ ngày" Style="{StaticResource Label}"/>
                    <DatePicker SelectedDate="{Binding TuNgay}"/>
                </StackPanel>
                <StackPanel Width="140">
                    <TextBlock Text="Đến ngày" Style="{StaticResource Label}"/>
                    <DatePicker SelectedDate="{Binding DenNgay}"/>
                </StackPanel>
            </WrapPanel>
        </Border>

        <!-- Grid -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid ItemsSource="{Binding HoaDons}" SelectedItem="{Binding Selected}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã HĐ"     Binding="{Binding MaHD}"    Width="70"/>
                    <DataGridTextColumn Header="Khách Hàng" Binding="{Binding DatPhong.KhachHang.HoTen}" Width="*"/>
                    <DataGridTextColumn Header="Phòng"     Binding="{Binding DatPhong.Phong.SoPhong}"    Width="80"/>
                    <DataGridTextColumn Header="Nhân Viên" Binding="{Binding NhanVien.HoTen}"            Width="*"/>
                    <DataGridTextColumn Header="Ngày Lập"  Binding="{Binding NgayLap, StringFormat=dd/MM/yyyy HH:mm}" Width="140"/>
                    <DataGridTextColumn Header="Số Ngày"    Binding="{Binding SoNgay, StringFormat={}{0} đêm}" Width="90"/>
                    <DataGridTextColumn Header="Còn Lại" Binding="{Binding ConLai, Converter={StaticResource Currency}}" Width="130"/>
                    <DataGridTextColumn Header="Phương Thức" Binding="{Binding PhuongThucTT, Converter={StaticResource PTText}}" Width="130"/>
                    <DataGridTemplateColumn Header="Trạng Thái" Width="150" SortMemberPath="TrangThai">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding TrangThai, Converter={StaticResource TTColor}}"
                                        CornerRadius="12" Padding="10,3" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding TrangThai, Converter={StaticResource TTText}}"
                                               Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Total -->
        <Border Grid.Row="3" Style="{StaticResource CardSmall}" Margin="0,12,0,0">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <TextBlock Text="Tổng Doanh Thu (đã TT):" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}" FontSize="15"
                               VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBlock Text="{Binding TongDoanhThu, Converter={StaticResource Currency}}"
                               FontSize="20" FontWeight="Bold"
                               Foreground="{StaticResource SuccessBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
