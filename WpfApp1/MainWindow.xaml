<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quản Lý Khách Sạn" Height="800" Width="1300"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
    FontFamily="Segoe UI">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <!-- Cửa sổ viền bo tròn đổ bóng -->
    <Border CornerRadius="12" Background="{StaticResource MainBackground}" BorderBrush="#CBD5E1" BorderThickness="1" ClipToBounds="True">
        <Grid>
            <Grid.ColumnDefinitions>
                <!-- Sidebar -->
                <ColumnDefinition Width="260"/>
                <!-- Main Content -->
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 1. SIDEBAR -->
            <Border Grid.Column="0" Background="{StaticResource SidebarBackground}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Khối Logo & User -->
                        <RowDefinition Height="*"/>    <!-- Menu -->
                        <RowDefinition Height="Auto"/> <!-- Khối Logout -->
                    </Grid.RowDefinitions>

                    <!-- Khối thông tin Logo & Avatar -->
                    <StackPanel Grid.Row="0" Margin="0,30,0,20" HorizontalAlignment="Center">
                        <!-- Icon Khách sạn ảo -->
                        <Border Width="60" Height="60" CornerRadius="30" Background="#141842" Margin="0,0,0,10">
                            <TextBlock Text="🏨" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
                        </Border>
                        <TextBlock Text="GRAND HOTEL" FontSize="16" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                        <TextBlock Text="Quản Lý Khách Sạn" FontSize="11" Foreground="#A0A5CA" HorizontalAlignment="Center" Margin="0,2,0,15"/>
                        
                        <!-- Info User -->
                        <Border Background="#2A2F72" CornerRadius="8" Padding="15,10" Width="220">
                            <StackPanel>
                                <TextBlock Text="{Binding UserFullName}" FontSize="14" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="{Binding UserRoleDisplay}" FontSize="12" Foreground="#A0A5CA" Margin="0,3,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Khối MENU -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="0,10,0,0">
                            
                            <Button Style="{StaticResource SidebarButton}" Content="📊  Tổng Quan" Click="BtnDashboard_Click"/>
                            
                            <Border Height="1" Background="#2A2F72" Margin="20,15,20,15"/>
                            <TextBlock Text="DANH MỤC PHÒNG" Foreground="#A0A5CA" FontSize="11" FontWeight="Bold" Margin="25,0,0,10"/>
                            
                            <Button Style="{StaticResource SidebarButton}" Content="🛏️  Quản Lý Phòng" Click="BtnPhong_Click"/>
                            <Button Style="{StaticResource SidebarButton}" Content="📋  Loại Phòng" Click="BtnLoaiPhong_Click"/>
                            
                            <Border Height="1" Background="#2A2F72" Margin="20,15,20,15"/>
                            <TextBlock Text="NGHIỆP VỤ" Foreground="#A0A5CA" FontSize="11" FontWeight="Bold" Margin="25,0,0,10"/>
                            
                            <Button Style="{StaticResource SidebarButton}" Content="👥  Khách Hàng" Click="BtnKhachHang_Click"/>
                            <Button Style="{StaticResource SidebarButton}" Content="🔑  Đặt Phòng" Click="BtnBooking_Click"/>
                            <Button Style="{StaticResource SidebarButton}" Content="🧾  Hóa Đơn" Click="BtnHoaDon_Click"/>
                            
                            <Border Height="1" Background="#2A2F72" Margin="20,15,20,15"/>
                            <TextBlock Text="QUẢN LÝ" Foreground="#A0A5CA" FontSize="11" FontWeight="Bold" Margin="25,0,0,10"/>
                            
                            <Button Style="{StaticResource SidebarButton}" Content="📈  Báo Cáo" Click="BtnBaoCao_Click" Visibility="{Binding ShowBaoCao, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            <Button Style="{StaticResource SidebarButton}" Content="⚙️  Cài Đặt" Click="BtnCaiDat_Click"/>
                            <Button Style="{StaticResource SidebarButton}" Content="🛡️  Phân Quyền" Click="BtnPhanQuyen_Click" Visibility="{Binding ShowPhanQuyen, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Khối Đăng xuất -->
                    <Border Grid.Row="2" Margin="0,10,0,20">
                        <Button Style="{StaticResource SidebarButton}" Content="⏏️  Đăng Xuất" Foreground="#EF4444" Click="BtnLogout_Click"/>
                    </Border>
                </Grid>
            </Border>

            <!-- 2. MAIN CONTENT AREA -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"/> <!-- Topbar -->
                    <RowDefinition Height="*"/>  <!-- Nội dung -->
                </Grid.RowDefinitions>

                <!-- TOPBAR -->
                <Border Grid.Row="0" Background="White" MouseDown="TopBar_MouseDown">
                    <Grid>
                        <TextBlock x:Name="TxtTitle" Text="Tổng Quan" FontSize="20" FontWeight="Bold" Foreground="#1E3C72" VerticalAlignment="Center" Margin="25,0,0,0"/>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0">
                            <!-- Đồng hồ thời gian thực -->
                            <TextBlock x:Name="TxtClock" Text="00:00:00" FontSize="14" FontWeight="SemiBold" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,20,0"/>
                            
                            <!-- Nút điều khiển cửa sổ -->
                            <Button Content="—" Style="{StaticResource TopBarButton}" Click="BtnMinimize_Click"/>
                            <Button Content="☐" Style="{StaticResource TopBarButton}" Click="BtnMaximize_Click"/>
                            <Button Content="✕" Style="{StaticResource TopBarCloseButton}" Click="BtnCloseWindow_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <Border Grid.Row="0" Height="1" Background="#E2E8F0" VerticalAlignment="Bottom"/> <!-- Line dưới Topbar -->

                <!-- NƠI LOAD CÁC PAGE CON -->
                <ContentControl x:Name="ContentArea" Grid.Row="1" Margin="20"/>
            </Grid>
        </Grid>
    </Border>
</Window>
