<Window x:Class="WpfApp1.GUI.CustomPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thông báo" SizeToContent="Height" Width="400" MinHeight="180"
        WindowStyle="None" Background="Transparent" AllowsTransparency="True"
        WindowStartupLocation="CenterOwner">
    <Border Background="White" CornerRadius="12" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.2"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Border Background="#3B82F6" CornerRadius="12,12,0,0" Padding="15">
                <TextBlock Text="🛠️ THÔNG BÁO HỆ THỐNG" Foreground="White" FontSize="16" FontWeight="Bold"/>
            </Border>
            
            <!-- Content -->
            <TextBlock Grid.Row="1" Text="Tính năng Báo cáo theo Loại phòng đang trong quá trình phát triển. Vui lòng chờ bản cập nhật tiếp theo nhé!" 
                       TextWrapping="Wrap" TextAlignment="Center" Margin="20" FontSize="14" Foreground="#1E293B" VerticalAlignment="Center"/>
            
            <!-- Button -->
            <Button Grid.Row="2" Content="Đã Hiểu" Width="120" Height="35" Margin="0,0,0,15" 
                    Background="#3B82F6" Foreground="White" BorderThickness="0" Cursor="Hand"
                    Click="BtnClose_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
    </Border>
</Window>
