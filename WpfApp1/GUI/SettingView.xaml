<UserControl x:Class="WpfApp1.GUI.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:WpfApp1.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="800"
             Background="Transparent">
    
    <UserControl.DataContext>
        <viewmodels:SettingViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource CardBlue}"/>
                                <Setter Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="{StaticResource CardBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1E40AF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <local:BoolToColorConverter x:Key="BoolToColorConverter" xmlns:local="clr-namespace:WpfApp1.GUI"/>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10,0,0,15">
            <TextBlock Text="Cài Đặt Quy Định" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
            <TextBlock Text="Tùy chỉnh thông số cốt lõi (QĐ1, QĐ2, QĐ4)" FontSize="14" Foreground="{StaticResource TextSecondary}"/>
        </StackPanel>

        <!-- Card nền trắng -->
        <Border Grid.Row="1" Style="{StaticResource ModernCard}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="600" Margin="10,0,0,0">
            <StackPanel Margin="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Cột trái -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Tỷ lệ phụ thu khách thứ 3 (%)" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding PhuThuKhachThu3Str, UpdateSourceTrigger=PropertyChanged}" ToolTip="Ví dụ: 25"/>

                        <TextBlock Text="Số khách tối đa / Phòng" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding SoKhachToiDaStr, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Tỷ lệ cọc tối thiểu (%)" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding TyLeTienCocStr, UpdateSourceTrigger=PropertyChanged}" ToolTip="Ví dụ: 30"/>
                    </StackPanel>

                    <!-- Cột phải -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Giờ nhận phòng (Check-in)" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding GioNhanPhongStr, UpdateSourceTrigger=PropertyChanged}" ToolTip="Định dạng hh:mm (VD: 14:00)"/>

                        <TextBlock Text="Giờ trả phòng (Check-out)" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding GioTraPhongStr, UpdateSourceTrigger=PropertyChanged}" ToolTip="Định dạng hh:mm (VD: 12:00)"/>
                    </StackPanel>
                </Grid>

                <Border Height="1" Background="#E2E8F0" Margin="0,10,0,20"/>

                <Grid>
                    <Button Content="💾 LƯU CẤU HÌNH" 
                            Command="{Binding SaveCommand}" 
                            Style="{StaticResource PrimaryButton}" 
                            Width="250" HorizontalAlignment="Left"/>

                    <!-- Thông báo -->
                    <TextBlock Text="{Binding Message}" 
                               FontSize="14" FontWeight="SemiBold" 
                               TextWrapping="Wrap"
                               HorizontalAlignment="Right" VerticalAlignment="Center"
                               Foreground="{Binding IsError, Converter={StaticResource BoolToColorConverter}}"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
